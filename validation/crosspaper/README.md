# Cross-paper verification harnesses — surfaced 2026-07-31

`verify_claims.py`: asserts manuscript numbers against artifacts (exists because a
0.491-vs-0.310 supplement error survived three review passes). `site_matched_cuts.py`:
re-derives every reported cut of the site-matched depth control. Both were written
2026-07-31 in the papers workspace; this is now the canonical copy — edit here first.
